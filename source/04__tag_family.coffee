## this should combine with helix-base

tag.define('tag-family', {
    extends: ''

    lookUp: (tagName) =>
        """Given a tagName, return the path to it.
        """

    lookUpRoot: (tagName) =>
        return

    parse: (link) =>
        return

    ## defined at tag level

})
